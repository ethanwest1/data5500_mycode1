cat > .gitignore <<'EOF'
# ---- Project-friendly blacklist (.git keeps tracking new stuff) ----

# OS/editor/log junk
*.log
code-server.log

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
.env

# Node (if ever used)
node_modules/

# VS Code
.vscode/

# --- Home-directory dotfiles/dirs (clutter you don't want on GitHub)
# (root-anchored so the same-named items inside your projects are unaffected)
/.cache/
/.config/
/.local/
/.ssh/
/.bash_history
/.bashrc
/.bash_logout
/.profile
/.sudo_as_admin_successful
/.wget-hsts

# Keep these visible
!.gitignore
EOF